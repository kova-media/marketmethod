import {NextRequest,NextResponse} from 'next/server'
import {getDb} from '../../../../lib/db'
import {getSession} from '../../../../lib/auth'
import {ensureSchema} from '../../../../lib/schema'
export async function GET(req:NextRequest){await ensureSchema();const s=getSession();if(!s)return NextResponse.json({error:'Unauthorized'},{status:401});const contactId=new URL(req.url).searchParams.get('contactId');const sql=getDb();const rows=contactId?await sql`select p.*,c.first_name,c.last_name from properties p join contacts c on c.id=p.contact_id where p.organization_id=${s.organizationId} and p.contact_id=${contactId} order by p.created_at desc`:await sql`select p.*,c.first_name,c.last_name from properties p join contacts c on c.id=p.contact_id where p.organization_id=${s.organizationId} order by p.created_at desc limit 500`;return NextResponse.json({properties:rows})}
export async function POST(req:NextRequest){await ensureSchema();const s=getSession();if(!s)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();if(!b.contactId||!b.address)return NextResponse.json({error:'Customer and address are required'},{status:400});const sql=getDb();const c=await sql`select id from contacts where id=${b.contactId} and organization_id=${s.organizationId}`;if(!c[0])return NextResponse.json({error:'Customer not found'},{status:404});const rows=await sql`insert into properties(organization_id,contact_id,name,address,city,state,postal_code,notes) values(${s.organizationId},${b.contactId},${b.name||null},${b.address},${b.city||null},${b.state||null},${b.postalCode||null},${b.notes||null}) returning *`;return NextResponse.json({property:rows[0]},{status:201})}
