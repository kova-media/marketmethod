import {NextRequest,NextResponse} from 'next/server'
import {getDb} from '../../../../lib/db'
import {getSession} from '../../../../lib/auth'
import {ensureSchema} from '../../../../lib/schema'
export async function GET(){await ensureSchema();const s=getSession();if(!s)return NextResponse.json({error:'Unauthorized'},{status:401});const sql=getDb();const [org,users,stages]=await Promise.all([sql`select id,name,slug,industry,logo_url,primary_color from organizations where id=${s.organizationId}`,sql`select id,name,email,role,created_at from users where organization_id=${s.organizationId} order by created_at`,sql`select id,name,position,color from pipeline_stages where organization_id=${s.organizationId} order by position`]);return NextResponse.json({organization:org[0],users,stages})}
export async function PATCH(req:NextRequest){await ensureSchema();const s=getSession();if(!s)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();const sql=getDb();const rows=await sql`update organizations set name=${b.name?.trim()||'Business'},industry=${b.industry?.trim()||null},primary_color=${b.primaryColor||'#C7ED63'},logo_url=${b.logoUrl?.trim()||null} where id=${s.organizationId} returning id,name,slug,industry,logo_url,primary_color`;return NextResponse.json({organization:rows[0]})}
