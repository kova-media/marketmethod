'use client'
import {FormEvent,useEffect,useMemo,useState} from 'react'
import {ChevronLeft,Plus,Search,X} from 'lucide-react'
const stages=['new','contacted','qualified','won']
export default function ContactsPage(){
 const [contacts,setContacts]=useState<any[]>([]),[query,setQuery]=useState(''),[status,setStatus]=useState('all'),[show,setShow]=useState(false),[selected,setSelected]=useState<any>(null),[form,setForm]=useState({firstName:'',lastName:'',email:'',phone:'',company:'',status:'new',notes:''})
 async function load(){const r=await fetch('/api/crm/contacts');const d=await r.json();if(r.ok)setContacts(d.contacts||[])}
 useEffect(()=>{load()},[])
 const filtered=useMemo(()=>contacts.filter(c=>[c.first_name,c.last_name,c.email,c.company].filter(Boolean).join(' ').toLowerCase().includes(query.toLowerCase())&&(status==='all'||(c.status||'new')===status)),[contacts,query,status])
 async function add(e:FormEvent){e.preventDefault();const r=await fetch('/api/crm/contacts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(r.ok){setContacts(p=>[d.contact,...p]);setShow(false);setForm({firstName:'',lastName:'',email:'',phone:'',company:'',status:'new',notes:''})}}
 return <main className="module-page"><header className="module-top"><div><a href="/crm/dashboard" className="back-link"><ChevronLeft size={15}/> Dashboard</a><span className="eyebrow">CUSTOMER SYSTEM</span><h1>Contacts</h1><p>Every lead and customer in one place.</p></div><button className="add-button" onClick={()=>setShow(true)}><Plus size={17}/> Add contact</button></header><section className="module-toolbar"><label className="module-search"><Search size={16}/><input placeholder="Search contacts..." value={query} onChange={e=>setQuery(e.target.value)}/></label><div className="filter-pills">{['all',...stages].map(x=><button key={x} className={status===x?'selected':''} onClick={()=>setStatus(x)}>{x==='all'?'All':x[0].toUpperCase()+x.slice(1)}</button>)}</div><span className="result-count">{filtered.length} contacts</span></section><section className="contact-list">{filtered.length?filtered.map(c=><button className="contact-row" key={c.id} onClick={()=>setSelected(c)}><span className="contact-avatar">{[c.first_name,c.last_name].filter(Boolean).map((x:string)=>x[0]).join('').toUpperCase()}</span><span className="contact-main"><strong>{c.first_name} {c.last_name||''}</strong><small>{c.company||c.email||c.phone||'No details'}</small></span><span className="contact-source">{c.source||'Manual'}</span><span className={'contact-status '+(c.status||'new')}>{(c.status||'new')[0].toUpperCase()+(c.status||'new').slice(1)}</span><span>›</span></button>):<div className="empty-module"><h2>No contacts yet</h2><p>Add your first customer or submit a lead through the public website.</p><button className="add-button" onClick={()=>setShow(true)}><Plus size={17}/> Add contact</button></div>}</section>{show&&<div className="modal-overlay" onClick={()=>setShow(false)}><form className="add-modal module-modal" onSubmit={add} onClick={e=>e.stopPropagation()}><button type="button" className="modal-close" onClick={()=>setShow(false)}><X size={18}/></button><span className="eyebrow">NEW CONTACT</span><h2>Add a contact</h2><div className="modal-form"><input placeholder="First name" value={form.firstName} onChange={e=>setForm({...form,firstName:e.target.value})} required/><input placeholder="Last name" value={form.lastName} onChange={e=>setForm({...form,lastName:e.target.value})}/><input type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input placeholder="Phone" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><input placeholder="Company or property" value={form.company} onChange={e=>setForm({...form,company:e.target.value})}/><select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}>{stages.map(x=><option key={x}>{x}</option>)}</select><textarea placeholder="Notes" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/><button className="add-button">Create contact</button></div></form></div>}{selected&&<ContactDrawer contact={selected} close={()=>setSelected(null)} saved={c=>{setContacts(p=>p.map(x=>x.id===c.id?c:x));setSelected(c)}}/>}</main>
}
function ContactDrawer({contact,close,saved}:{contact:any,close:()=>void,saved:(c:any)=>void}){const [form,setForm]=useState({firstName:contact.first_name,lastName:contact.last_name||'',email:contact.email||'',phone:contact.phone||'',company:contact.company||'',status:contact.status||'new',notes:contact.notes||''});const [activities,setActivities]=useState<any[]>([]);const [note,setNote]=useState('');useEffect(()=>{fetch('/api/crm/activities?contactId='+contact.id).then(r=>r.json()).then(d=>setActivities(d.activities||[]))},[contact.id]);async function save(){const r=await fetch('/api/crm/contacts',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:contact.id,...form})});const d=await r.json();if(r.ok)saved(d.contact)}async function addNote(){if(!note.trim())return;const r=await fetch('/api/crm/activities',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contactId:contact.id,title:'Note added',body:note})});const d=await r.json();if(r.ok){setActivities(p=>[d.activity,...p]);setNote('')}}return <div className="drawer-overlay" onClick={close}><aside className="contact-drawer" onClick={e=>e.stopPropagation()}><header><div><span className="eyebrow">CONTACT</span><h2>{contact.first_name} {contact.last_name||''}</h2></div><button className="modal-close" onClick={close}><X size={18}/></button></header><div className="drawer-body"><section className="drawer-section"><h3>Details</h3><div className="drawer-fields"><input value={form.firstName} onChange={e=>setForm({...form,firstName:e.target.value})}/><input value={form.lastName} onChange={e=>setForm({...form,lastName:e.target.value})}/><input value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><input value={form.company} onChange={e=>setForm({...form,company:e.target.value})}/><select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}>{stages.map(x=><option key={x}>{x}</option>)}</select><textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></div><button className="add-button" onClick={save}>Save changes</button></section><section className="drawer-section"><h3>Activity</h3><div className="note-box"><textarea placeholder="Add a note..." value={note} onChange={e=>setNote(e.target.value)}/><button onClick={addNote}>Add note</button></div><div className="activity-list">{activities.map(a=><article key={a.id}><span className="activity-dot"/><div><strong>{a.title}</strong><p>{a.body}</p><small>{new Date(a.created_at).toLocaleString()}</small></div></article>)}</div></section></div></aside></div>}
