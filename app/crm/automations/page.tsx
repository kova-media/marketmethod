'use client'
import {FormEvent,useEffect,useState} from 'react'
import {ChevronLeft,Plus,X} from 'lucide-react'
const triggers=[['contact_created','New contact created'],['status_changed','Pipeline status changes'],['appointment_completed','Appointment completed']]
export default function AutomationsPage(){const [items,setItems]=useState<any[]>([]),[events,setEvents]=useState<any[]>([]),[show,setShow]=useState(false),[form,setForm]=useState({name:'',triggerType:'contact_created',taskTitle:'Follow up with new contact',taskDays:'1'});useEffect(()=>{load()},[]);async function load(){const r=await fetch('/api/crm/automations');const d=await r.json();if(r.ok){setItems(d.automations||[]);setEvents(d.events||[])}}async function add(e:FormEvent){e.preventDefault();const r=await fetch('/api/crm/automations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:form.name,triggerType:form.triggerType,actions:[{type:'create_task',title:form.taskTitle,dueAt:Number(form.taskDays)}]})});const d=await r.json();if(r.ok){setItems(p=>[d.automation,...p]);setShow(false)}}async function toggle(id:string,enabled:boolean){const r=await fetch('/api/crm/automations',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,enabled})});const d=await r.json();if(r.ok)setItems(p=>p.map(x=>x.id===id?d.automation:x))}return <main className="module-page"><header className="module-top"><div><a href="/crm/dashboard" className="back-link"><ChevronLeft size={15}/> Dashboard</a><span className="eyebrow">AUTOMATION</span><h1>Automations</h1><p>Turn repeatable follow-up into rules the system can run for you.</p></div><button className="add-button" onClick={()=>setShow(true)}><Plus size={17}/> New automation</button></header><div className="settings-grid"><section className="panel settings-panel"><div className="panel-head"><div><span className="eyebrow">RULES</span><h3>Active automations</h3></div><span className="count-badge">{items.length}</span></div>{items.map(a=><div className="automation-row" key={a.id}><div><strong>{a.name}</strong><small>{triggers.find(x=>x[0]===a.trigger_type)?.[1]||a.trigger_type}</small></div><button className={'toggle '+(a.enabled?'on':'')} onClick={()=>toggle(a.id,!a.enabled)}><i/></button></div>)}{!items.length&&<div className="soft-empty">No automation rules yet.</div>}</section><section className="panel settings-panel"><div className="panel-head"><div><span className="eyebrow">ACTIVITY</span><h3>Recent runs</h3></div></div>{events.map(e=><div className="automation-row" key={e.id}><div><strong>{e.automation_name}</strong><small>{e.event_type} · {new Date(e.created_at).toLocaleString()}</small></div><em className={e.status==='failed'?'error-text':'success-text'}>{e.status}</em></div>)}{!events.length&&<div className="soft-empty">No automation runs yet.</div>}</section></div>{show&&<div className="modal-overlay" onClick={()=>setShow(false)}><form className="add-modal module-modal" onSubmit={add} onClick={e=>e.stopPropagation()}><button type="button" className="modal-close" onClick={()=>setShow(false)}><X size={18}/></button><span className="eyebrow">NEW RULE</span><h2>Create automation</h2><div className="modal-form"><input placeholder="Automation name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><select value={form.triggerType} onChange={e=>setForm({...form,triggerType:e.target.value})}>{triggers.map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</select><input placeholder="Task to create" value={form.taskTitle} onChange={e=>setForm({...form,taskTitle:e.target.value})} required/><input type="number" min="0" placeholder="Days until task is due" value={form.taskDays} onChange={e=>setForm({...form,taskDays:e.target.value})}/><button className="add-button">Create automation</button></div></form></div>}</main>}
