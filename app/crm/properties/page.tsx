'use client'
import {FormEvent,useEffect,useState} from 'react'
import {ChevronLeft,Plus,X} from 'lucide-react'
export default function PropertiesPage(){const [contacts,setContacts]=useState<any[]>([]),[properties,setProperties]=useState<any[]>([]),[history,setHistory]=useState<any[]>([]),[showProperty,setShowProperty]=useState(false),[showService,setShowService]=useState(false),[property,setProperty]=useState({contactId:'',name:'',address:'',city:'',state:'',postalCode:''}),[service,setService]=useState({contactId:'',propertyId:'',serviceType:'',serviceDate:'',amount:'',nextRecommendedDate:'',notes:''});useEffect(()=>{Promise.all([fetch('/api/crm/contacts'),fetch('/api/crm/properties'),fetch('/api/crm/service-history')]).then(async ([a,b,c])=>{const ad=await a.json(),bd=await b.json(),cd=await c.json();if(a.ok)setContacts(ad.contacts||[]);if(b.ok)setProperties(bd.properties||[]);if(c.ok)setHistory(cd.history||[])})},[]);async function addProperty(e:FormEvent){e.preventDefault();const r=await fetch('/api/crm/properties',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(property)});const d=await r.json();if(r.ok){setProperties(p=>[d.property,...p]);setShowProperty(false)}}async function addService(e:FormEvent){e.preventDefault();const r=await fetch('/api/crm/service-history',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(service)});const d=await r.json();if(r.ok){setHistory(p=>[d.record,...p]);setShowService(false)}}return <main className="module-page"><header className="module-top"><div><a href="/crm/dashboard" className="back-link"><ChevronLeft size={15}/> Dashboard</a><span className="eyebrow">PROPERTY SERVICES</span><h1>Properties & service</h1><p>Properties, service history, and future service opportunities.</p></div><div><button className="add-button" onClick={()=>setShowProperty(true)}><Plus size={17}/> Add property</button> <button className="add-button" onClick={()=>setShowService(true)}>Record service</button></div></header><div className="dashboard-grid"><section className="panel"><div className="panel-head"><div><span className="eyebrow">PROPERTIES</span><h3>{properties.length} properties</h3></div></div>{properties.map(p=><div className="settings-row" key={p.id}><span className="stage-number">P</span><span><strong>{p.name||p.address}</strong><small>{p.first_name} {p.last_name||''} · {p.address}</small></span><em>{p.city||''} {p.state||''}</em></div>)}{!properties.length&&<div className="soft-empty">No properties added yet.</div>}</section><section className="panel"><div className="panel-head"><div><span className="eyebrow">SERVICE HISTORY</span><h3>{history.length} records</h3></div></div>{history.map(h=><div className="settings-row" key={h.id}><span className="stage-number">S</span><span><strong>{h.service_type}</strong><small>{h.first_name} {h.last_name||''} · {h.name||h.address||'Property'}</small></span><em>{h.next_recommended_date?'Next '+new Date(h.next_recommended_date).toLocaleDateString():''}</em></div>)}{!history.length&&<div className="soft-empty">No service records yet.</div>}</section></div>{showProperty&&<div className="modal-overlay" onClick={()=>setShowProperty(false)}><form className="add-modal module-modal" onSubmit={addProperty} onClick={e=>e.stopPropagation()}><button type="button" className="modal-close" onClick={()=>setShowProperty(false)}><X size={18}/></button><span className="eyebrow">PROPERTY</span><h2>Add property</h2><div className="modal-form"><select value={property.contactId} onChange={e=>setProperty({...property,contactId:e.target.value})} required><option value="">Choose customer</option>{contacts.map(c=><option key={c.id} value={c.id}>{c.first_name} {c.last_name||''}</option>)}</select><input placeholder="Property name" value={property.name} onChange={e=>setProperty({...property,name:e.target.value})}/><input placeholder="Street address" value={property.address} onChange={e=>setProperty({...property,address:e.target.value})} required/><input placeholder="City" value={property.city} onChange={e=>setProperty({...property,city:e.target.value})}/><input placeholder="State" value={property.state} onChange={e=>setProperty({...property,state:e.target.value})}/><input placeholder="Postal code" value={property.postalCode} onChange={e=>setProperty({...property,postalCode:e.target.value})}/><button className="add-button">Add property</button></div></form></div>}{showService&&<div className="modal-overlay" onClick={()=>setShowService(false)}><form className="add-modal module-modal" onSubmit={addService} onClick={e=>e.stopPropagation()}><button type="button" className="modal-close" onClick={()=>setShowService(false)}><X size={18}/></button><span className="eyebrow">SERVICE</span><h2>Record service</h2><div className="modal-form"><select value={service.contactId} onChange={e=>setService({...service,contactId:e.target.value})} required><option value="">Choose customer</option>{contacts.map(c=><option key={c.id} value={c.id}>{c.first_name} {c.last_name||''}</option>)}</select><select value={service.propertyId} onChange={e=>setService({...service,propertyId:e.target.value})}><option value="">Choose property</option>{properties.filter(p=>!service.contactId||p.contact_id===service.contactId).map(p=><option key={p.id} value={p.id}>{p.name||p.address}</option>)}</select><input placeholder="Service type" value={service.serviceType} onChange={e=>setService({...service,serviceType:e.target.value})} required/><input type="date" value={service.serviceDate} onChange={e=>setService({...service,serviceDate:e.target.value})}/><input placeholder="Amount" value={service.amount} onChange={e=>setService({...service,amount:e.target.value})}/><input type="date" value={service.nextRecommendedDate} onChange={e=>setService({...service,nextRecommendedDate:e.target.value})}/><textarea placeholder="Notes" value={service.notes} onChange={e=>setService({...service,notes:e.target.value})}/><button className="add-button">Record service</button></div></form></div>}</main>}
