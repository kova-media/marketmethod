'use client'
import {useEffect,useState} from 'react'
import {ChevronLeft,MessageSquare,Plus,Send,X} from 'lucide-react'
export default function ConversationsPage(){const [items,setItems]=useState<any[]>([]),[contacts,setContacts]=useState<any[]>([]),[selected,setSelected]=useState<any>(null),[messages,setMessages]=useState<any[]>([]),[body,setBody]=useState(''),[subject,setSubject]=useState(''),[error,setError]=useState(''),[show,setShow]=useState(false),[contactId,setContactId]=useState(''),[channel,setChannel]=useState('sms');useEffect(()=>{load()},[]);async function load(){const [a,b]=await Promise.all([fetch('/api/crm/conversations'),fetch('/api/crm/contacts')]);const ad=await a.json(),bd=await b.json();if(a.ok)setItems(ad.conversations||[]);if(b.ok)setContacts(bd.contacts||[])}async function open(c:any){setSelected(c);const r=await fetch('/api/crm/conversations/'+c.id);const d=await r.json();if(r.ok)setMessages(d.messages||[])}async function create(){if(!contactId)return;const r=await fetch('/api/crm/conversations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contactId,channel})});const d=await r.json();if(r.ok){setShow(false);await load();const found={...d.conversation,contact_id:contactId,first_name:contacts.find(c=>c.id===contactId)?.first_name,last_name:contacts.find(c=>c.id===contactId)?.last_name,channel};open(found)}}async function send(){if(!body.trim()||!selected)return;setError('');const r=await fetch('/api/crm/conversations/'+selected.id,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({body,subject})});const d=await r.json();if(r.ok){setMessages(p=>[...p,d.message]);setBody('');setSubject('')}else setError(d.error||'Message could not be sent.')}return <main className="module-page"><header className="module-top"><div><a href="/crm/dashboard" className="back-link"><ChevronLeft size={15}/> Dashboard</a><span className="eyebrow">CUSTOMER COMMUNICATION</span><h1>Conversations</h1><p>Keep messages attached to the customer record.</p></div><button className="add-button" onClick={()=>setShow(true)}><Plus size={17}/> Start conversation</button></header><div className="conversation-layout"><aside className="conversation-list">{items.map(c=><button key={c.id} className={selected?.id===c.id?'selected':''} onClick={()=>open(c)}><span className="contact-avatar">{[c.first_name,c.last_name].filter(Boolean).map((x:string)=>x[0]).join('').toUpperCase()}</span><span><strong>{c.first_name} {c.last_name||''}</strong><small>{c.channel.toUpperCase()} · {c.status}</small></span></button>)}{!items.length&&<div className="soft-empty">No conversations yet.</div>}</aside><section className="conversation-main">{selected?<><header><span className="eyebrow">{selected.channel.toUpperCase()}</span><h2>{selected.first_name} {selected.last_name||''}</h2></header><div className="message-list">{messages.map(m=><div className={'message-bubble '+m.direction} key={m.id}>{m.body}<small>{new Date(m.sent_at).toLocaleString()}</small></div>)}{!messages.length&&<div className="soft-empty">No messages yet. This is the record where SMS and email integrations will connect.</div>}</div><div className="message-compose">{selected.channel==='email'&&<input className="message-subject" value={subject} onChange={e=>setSubject(e.target.value)} placeholder="Subject"/>}{error&&<div className="form-error">{error}</div>}<textarea value={body} onChange={e=>setBody(e.target.value)} placeholder="Write a message..."/><button onClick={send}><Send size={16}/></button></div></>:<div className="conversation-empty"><MessageSquare size={30}/><h2>Select a conversation</h2><p>Customer messages will live here.</p></div>}</section></div>{show&&<div className="modal-overlay" onClick={()=>setShow(false)}><div className="add-modal module-modal" onClick={e=>e.stopPropagation()}><button className="modal-close" onClick={()=>setShow(false)}><X size={18}/></button><span className="eyebrow">NEW CONVERSATION</span><h2>Start a conversation</h2><div className="modal-form"><select value={contactId} onChange={e=>setContactId(e.target.value)}><option value="">Choose contact</option>{contacts.map(c=><option key={c.id} value={c.id}>{c.first_name} {c.last_name||''}</option>)}</select><select value={channel} onChange={e=>setChannel(e.target.value)}><option value="sms">SMS</option><option value="email">Email</option></select><button className="add-button" onClick={create}>Create conversation</button></div></div></div>}</main>}
